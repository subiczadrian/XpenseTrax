name: Deploy XpenseTrax to the server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy project
        run: |
          echo "${{ secrets.PSWD }}" | sudo rm -rf /var/XpenseTrax/
          echo "${{ secrets.PSWD }}" | sudo rm -rf /home/XpenseTrax/
          cd /home/
          echo "${{ secrets.PSWD }}" | sudo git clone https://github.com/subiczadrian/XpenseTrax XpenseTrax

          cd /home/XpenseTrax/backend/XpenseTrax.API/
          echo "${{ secrets.PSWD }}" | sudo dotnet publish -o /var/XpenseTrax/backend/publish

          echo "${{ secrets.PSWD }}" | sudo cp -r /home/XpenseTrax/frontend /var/XpenseTrax/frontend

          echo "Deployment complete!"
